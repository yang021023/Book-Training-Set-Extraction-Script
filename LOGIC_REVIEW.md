# 脚本逻辑检查记录

检查日期：2026-09-05。范围：`manufacturing-document-qa/scripts` 下全部 13 个脚本，以及 README、SKILL.md 和 introduce.txt 中的流程约定。

本次完成代码审查和临时样本验证，尚未修改业务脚本。没有用清洗脚本处理真实训练数据。

1. **高优先级：提交失败后，数据与进度无法一起恢复。**

   位置：`qa-work.ps1:369`、`:380`、`:433`、`:453`。
   Commit/Augment 先替换 JSONL，再保存 state.json，两次文件写入不构成一个事务，与 SKILL.md 所称“原子更新”不一致。
   已复现：人为使状态临时文件不可写后，JSONL 已增加 3 条，state 仍是 `next_page=1, record_count=0`；解除障碍后原批次重试，又报“批次没有新增记录，拒绝推进页码”。
   建议：引入可恢复的提交日志及提交标识，在恢复或重试时完成未完成的状态更新；单独给两个文件使用临时文件仍不能解决双文件一致性。

2. **高优先级：清洗把具体实例的参数改成了缺少适用条件的结论。**

   位置：`clean-modern-cnc-design.ps1:23`、`:24`；`clean-practical-machining-handbook.ps1:53`、`:60`；`clean-metal-cutting-principles.ps1:41`。
   规则删去了机型、案例或产品背景，但保留固定规格。例如，未指定型号的“车削中心刀架 Y 轴行程”被回答为 85 mm；泛称“齿轮轴选用什么材料”被回答为 40Cr；泛称齿轮轴磨削余量被回答为 0.6 mm。
   这些问答没有足够条件唯一确定答案。问题并非证明原书数值错误，而是改写后失去了数值成立的对象和工况。
   建议：保留必要型号、结构和工况；无法由原始资料补齐条件时，停止套用该改写，转为待核实或删除。

3. **高优先级：三个清洗脚本只按行号删改，不校验原内容。**

   位置：`clean-modern-cnc-design.ps1:73`、`:77`；`clean-metal-cutting-principles.ps1:103`、`:107`；`clean-practical-machining-handbook.ps1:156`、`:160`。
   输入发生插行、去重、排序或版本变化后，规则仍会命中原来的行号。
   已复现：构造 135 条无关记录，第 135 条仍被替换成预设的“刀具固定、工件运动的机床应怎样确定坐标轴正方向”。
   建议：为一次性补丁锁定源文件版本，并为每条规则核对预期原问题或记录哈希；不匹配时在写输出前失败。

4. **高优先级：清洗提前覆盖结果文件，失败时留下不完整结果。**

   位置：`clean-modern-cnc-design.ps1:59`、`:65`；`clean-metal-cutting-principles.ps1:89`、`:95`；`clean-practical-machining-handbook.ps1:142`、`:148`。
   三者均先以覆盖模式打开结果和决策文件，再读取、解析输入。源文件缺失或处理中途异常时，原结果已经被清空或替换为部分结果。
   另外，modern-cnc 和 metal-cutting 没有设置统一的停止错误策略。已在 modern-cnc 复现：输入一条正常记录和一条坏 JSON 后，输出仅剩一条，脚本仍打印汇总并以退出码 0 结束。
   建议：先检查输入，写入独立临时结果，全部成功后再发布；解析失败必须向调用方返回失败。

5. **高优先级：主脚本没有传递最终校验失败的退出码。**

   位置：`qa-work.ps1:321`。
   Validate 收集底层校验输出后直接 Emit，没有检查底层退出状态，也没有在 `valid=False` 时使主命令失败。
   已复现：JSONL 内容是 `not json`，主命令输出 `invalid_json=1, valid=False`，进程退出码仍为 0。依赖退出码的自动化会误判成功。
   建议：保留结构化报告，同时显式向上返回失败。

6. **中优先级：提交与最终校验使用了不同的数据契约。**

   位置：`qa-work.ps1:148`、`:165`；`validate-jsonl.ps1:40`、`:101`、`:113`。
   已复现：“装配图中如何标注配合尺寸？”被提交门禁误判为来源依赖，但最终校验通过；含 `.pdf` 的文本可通过提交记录检查，却被最终校验拒绝。
   Commit 要求恰好两条消息，最终校验却允许任意偶数条消息。最终校验还把 content 强转为字符串判断，数字 `123/456` 作为问答内容也得到 `valid=True`。
   建议：共享确定性的结构、类型和禁用模式检查；对“装配图中”等正常术语保留边界判断。

7. **中优先级：12 页上限只限制规划参数，没有限制实际提交。**

   位置：`qa-work.ps1:14`、`:341`、`:405`。
   Commit/Augment 根据批次文件计算页数，没有检查总页数是否超过 12。
   已复现：直接提交第 1—13 页、39 条记录，提交成功且书籍被标记为 completed。
   建议：在实际提交入口校验 1—12 页，避免只依赖调用者先执行 Plan。

8. **中优先级：连续非技术页缺少与说明一致的正常推进路径。**

   位置：`qa-work.ps1:419`；`SKILL.md:142`、`:146`。
   coverage 支持版权、目录等非技术页，但任何零新增记录的 Commit 都被拒绝；文档中的 Skip 又仅明确授权纯编号页场景。
   已复现：单页版权页、完整 nontechnical coverage、空 records，提交仍报“批次没有新增记录”。连续前言、目录或书末非技术页因此难以按现有约定独立完成。
   建议：明确非技术页的跳过规则、理由和记录方式；对全技术页零新增的情况继续保留质量检查。

9. **中优先级：清洗统计计算的是配置数量，不是实际执行数量。**

   位置：`clean-modern-cnc-design.ps1:111`、`:117`；`clean-practical-machining-handbook.ps1:191`、`:198`。
   两个脚本按 rewrites/deletes 字典总数统计，keep 则直接从输入行数减去字典长度。
   已复现：modern-cnc 输入 1 条记录，没有命中任何规则，却报告重写 23 条、删除 8 条、保留 -30 条。
   建议：按实际动作和原因累计，并核对输入数等于保留、改写和删除之和。

10. **中优先级：PDF 渲染的 WASM 路径格式与当前 Node 加载器不匹配。**

    位置：`render-pdf-page.mjs:211`。
    wasmUrl 传入的是 `file://` URL 字符串；当前锁定的 pdfjs-dist 6.2.108 在 NodeBinaryDataFactory 中把它直接交给 `fs.readFile`。Node 的这个接口接受本地路径或 URL 对象，不能把 URL 字符串当本地路径读。
    已验证：同一个 `jbig2.wasm` 文件用本地路径可读到 104852 字节，使用脚本构造方式得到的 URL 字符串读取则报 ENOENT。
    这会影响需要加载相关解码资源的页面。本次未用真实扫描书验证整页渲染结果。
    建议：根据 Node 数据工厂的契约传本地目录路径，并使用真实 JBIG2 扫描页做回归。

11. **中优先级：旧 JBIG2 对象读取器会删除有效二进制字节。**

    位置：`decode-jbig2-object.mjs:41`。
    读取器忽略 PDF 的 Length，按文本搜索 endstream，并删除流首尾所有空格、制表符和换行字节。压缩数据可以合法包含这些字节。
    已复现：Length=3、内容为字节 `[32,65,10]` 的流，返回的内容只有 `[65]`。
    主渲染脚本目前通过 `const jbig2 = null` 关闭了这个分支，因此此问题主要影响直接调用解码脚本，或以后重新启用旧分支的场景。
    建议：采用正确的 PDF 流长度和对象解析，或移除已经停用的旧路径，统一交给 PDF.js。

12. **中优先级：部分状态修改操作没有加入同一把锁。**

    位置：`qa-work.ps1:267`、`:308`。
    Commit/Augment/Skip 使用互斥锁；SetNext 和 Compact 则直接读取、写回状态。与提交并发时，后者可能用旧状态覆盖新进度或记录数；固定的 state.json.tmp 还可能互相干扰。
    这是代码确认的并发缺口，本次没有做时序压力复现。
    建议：所有状态写入共用互斥机制，并在获取锁后读取当前状态。

验证情况：现有 `test-domain-scope.ps1`、`test-qa-work-augment.ps1`、`test-qa-work-coverage.ps1`、`test-qa-work-low-yield.ps1` 四组测试均通过，三个 `.mjs` 均通过 Node 语法检查。上述临时样本覆盖了现有测试没有检查的提交故障、校验失败、规则漂移、异常输入、超长批次和二进制读取情况。

限制：未提供真实书籍 PDF 和原始训练 JSONL，因此没有核对清洗规则中的具体技术数值是否忠实于原书，也没有执行真实 OCR。`ocr-images.ps1` 与 `inspect-pdf.mjs` 本次未发现同等级的确定性逻辑缺陷；语法检查不等同于真实页面和 OCR 的端到端验证。

建议修复顺序：先处理提交恢复、清洗内容与输入保护、校验失败传播，再统一数据门禁和页码规则，最后处理渲染资源、旧解码器及并发状态写入。
